# Local development sample values only.
# In production, set these in the shell, system service, PM2, or Baota environment.
PUBLIC_POCKETBASE_URL=http://127.0.0.1:8090
# Local development only. Production CMS scripts no longer load admin credentials from
# project-root .env by default.
PB_ADMIN_EMAIL=admin@example.com
PB_ADMIN_PASSWORD=change-me
# Emergency local override for production-like environments. Leave unset by default.
# CMS_ALLOW_LOCAL_ADMIN_ENV=true
